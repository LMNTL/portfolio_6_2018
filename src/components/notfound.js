import React from 'react';

const NotFound = () => (
	<div className='NotFound'>
		<p>Look's like there's nothing here. Check that the address you followed to get here is correct or click on the navigation bar above.</p>
	</div>
);

export default NotFound;